{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "python -X utf8 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 4000",
    "start": "python -X utf8 -m uvicorn app.main:app --host 0.0.0.0 --port 4000",
    "test": "python -m pytest tests -q",
    "migrate:supabase-user": "python -m scripts.migrate_user_from_supabase"
  }
}
